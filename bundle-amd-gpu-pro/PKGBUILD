# Maintainer: Dankcuddlybear <dankcuddlybear@protonmail.com>
pkgname=bundle-amd-gpu-pro
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='AMD GPU drivers - all-in-one bundle with both open source and proprietary components (64bit+32bit)'
arch=(x86_64)
url='https://github.com/dankcuddlybear/zestiso'
depends=(bundle-amd-gpu
	## OpenGL (AUR Fresh Builds)
	amdgpu-pro-oglp lib32-amdgpu-pro-oglp
	## Vulkan (AUR Fresh Builds)
	vulkan-amdgpu-pro lib32-vulkan-amdgpu-pro
	## OpenCL (Chaotic-AUR)
	opencl-amd
	## AMF (Advanced Media Framework) video encoding (AUR Fresh Builds)
	amf-amdgpu-pro
)
